apiVersion: v1
kind: Service
metadata:
  name: hrms-directory-service
spec:
  selector:
    app: hrms-directory-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
